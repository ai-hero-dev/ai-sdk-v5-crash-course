# 03.05 MCP via SSE - Update Notes

## Issue
Current syntax causes "Method not allowed" error.

## Fix
Use SSE transport object syntax:

```ts
const mcpClient = await createMCPClient({
  transport: {
    type: 'sse',
    url: 'https://api.githubcopilot.com/mcp',
    headers: {
      Authorization: `Bearer ${process.env.GITHUB_PERSONAL_ACCESS_TOKEN}`,
    },
  },
});
```

## Dependencies
- `@modelcontextprotocol/sdk`

## Reference
https://ai-sdk.dev/docs/ai-sdk-core/mcp-tools#http-transport-recommended
